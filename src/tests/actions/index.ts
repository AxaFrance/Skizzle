import { clickButtonAsync, clickButton } from './clickButton';
import { changeTabAsync, changeTab } from './changeTab';
import { connectWith } from './connection';

const actions = {
  clickButtonAsync,
  clickButton,
  changeTabAsync,
  changeTab,
  connectWith
};

export default actions;
